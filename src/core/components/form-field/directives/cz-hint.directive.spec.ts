import { CzHintDirective } from './cz-hint.directive';

describe('CzHintDirective', () => {
  it('should create an instance', () => {
    const directive = new CzHintDirective();
    expect(directive).toBeTruthy();
  });
});
