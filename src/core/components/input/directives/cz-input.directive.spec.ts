import { CzInputDirective } from './cz-input.directive';

describe('CzInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CzInputDirective();
    expect(directive).toBeTruthy();
  });
});
