import { CzSliderItemDefDirective } from './cz-slider-item-def.directive';

describe('CzSliderItemDefDirective', () => {
  it('should create an instance', () => {
    const directive = new CzSliderItemDefDirective();
    expect(directive).toBeTruthy();
  });
});
